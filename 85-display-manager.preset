# (tpg)
# Enable all display managers by default.
# In most cases only one is installed.

enable sddm.service
enable gdm.service
enable lightdm.service
enable slim.service
enable lxdm.service
enable xdm.service
